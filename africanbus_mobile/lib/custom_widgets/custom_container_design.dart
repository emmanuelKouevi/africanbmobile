import 'package:flutter/material.dart';

class CustomContainerDesign extends StatelessWidget {
  const CustomContainerDesign({required Key key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
